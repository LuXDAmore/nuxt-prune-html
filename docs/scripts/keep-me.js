'use strict';

(
    function( window ) {

        window.console.info(
            '%c Nuxt Prune HTML ',
            'color: #fff; background-color: #333; padding: 4px 2px;',
            '🤘 Ready to rock'
        );

    }
)( window );
